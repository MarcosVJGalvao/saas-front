import TableBody from '@mui/material/TableBody';
import type { TableBodyProps } from '@mui/material/TableBody';

export type AppTableBodyProps = TableBodyProps;

export const AppTableBody = (props: AppTableBodyProps) => <TableBody {...props} />;
