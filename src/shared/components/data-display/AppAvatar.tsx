import Avatar from '@mui/material/Avatar';
import type { AvatarProps } from '@mui/material/Avatar';

export type AppAvatarProps = AvatarProps;

export const AppAvatar = (props: AppAvatarProps) => <Avatar {...props} />;
