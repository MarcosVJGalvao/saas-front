import TableRow from '@mui/material/TableRow';
import type { TableRowProps } from '@mui/material/TableRow';

export type AppTableRowProps = TableRowProps;

export const AppTableRow = (props: AppTableRowProps) => <TableRow {...props} />;
