import Typography from '@mui/material/Typography';
import type { TypographyProps } from '@mui/material/Typography';

export type AppTextProps = TypographyProps;

export const AppText = (props: AppTextProps) => <Typography {...props} />;
