import Divider from '@mui/material/Divider';
import type { DividerProps } from '@mui/material/Divider';

export type AppDividerProps = DividerProps;

export const AppDivider = (props: AppDividerProps) => <Divider {...props} />;
