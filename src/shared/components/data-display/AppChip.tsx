import Chip from '@mui/material/Chip';
import type { ChipProps } from '@mui/material/Chip';

export type AppChipProps = ChipProps;

export const AppChip = (props: AppChipProps) => <Chip {...props} />;
