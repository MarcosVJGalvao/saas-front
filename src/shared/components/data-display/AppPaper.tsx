import Paper from '@mui/material/Paper';
import type { PaperProps } from '@mui/material/Paper';

export type AppPaperProps = PaperProps;

export const AppPaper = (props: AppPaperProps) => <Paper {...props} />;
