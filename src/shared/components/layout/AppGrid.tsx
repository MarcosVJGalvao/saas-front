import Grid from '@mui/material/Grid';
import type { GridProps } from '@mui/material/Grid';

export type AppGridProps = GridProps;

export const AppGrid = (props: AppGridProps) => <Grid {...props} />;
