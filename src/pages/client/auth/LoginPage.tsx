import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';

const ClientLoginPage = () => (
  <Box>
    <Typography variant="h4">Login Client (em construção)</Typography>
  </Box>
);

export default ClientLoginPage;
